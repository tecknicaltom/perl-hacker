# Inspired by the VirtualEnv activate script
# This file must be used with "source bin/activate" *from bash*
# you cannot run it directly

THISDIR="$(dirname "$(realpath "$0")")"

PATH="$THISDIR:$PATH"
export PATH

PERL5LIB="$THISDIR/../lib:$PERL5LIB"
export PERL5LIB
